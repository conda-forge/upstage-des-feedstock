{% set name = "upstage-des" %}
{% set version = "0.4.0" %}
{% set python_min = "3.11" %}

package:
  name: {{ name|lower }}
  version: {{ version }}

source:
  url: https://pypi.org/packages/source/{{ name[0] }}/{{ name }}/upstage_des-{{ version }}.tar.gz
  sha256: 7467b69c3c6ee0089780c4584a57bbebd434db7141e414ac4ba9c299745e8c32

build:
  noarch: python
  script: {{ PYTHON }} -m pip install . -vv --no-deps --no-build-isolation
  number: 0

requirements:
  host:
    - python {{ python_min }}
    - flit-core >=3.7.1,<4
    - pip
  run:
    - python >={{ python_min }}
    - simpy >=4

test:
  imports:
    - upstage_des
  commands:
    - pip check
  requires:
    - python {{ python_min }}
    - pip

about:
  home: https://github.com/gtri/upstage
  summary: A library for behavior-driven discrete event simulation.
  dev_url: https://github.com/gtri/upstage
  license: BSD-3-Clause
  license_file: LICENSE
  doc_url: https://gtri.github.io/upstage/

extra:
  recipe-maintainers:
    - JamesArruda
